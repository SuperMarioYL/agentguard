# Publish blocked — pre-flight gate failed

`publish_repo.sh` refused to push `agentguard` to GitHub. Fix the issues below, then re-run.

- git push to existing origin failed (non-fast-forward? re-run with --force if intended).
